# Secrets and local MCP config
.env
.env.*
!.env.example
mcp.json
.cursor/mcp.json

# Backups and customer data (never commit)
backups/
docs/backups/
*.pem
*.key
credentials*

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# OS / IDE
.DS_Store
.idea/
*.swp

# Local clones of upstream MCP (install separately)
tools/meraki-magic-mcp-community/

# Bundled gh download (optional)
.cache/
